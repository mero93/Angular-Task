import { AnimateClickDirective } from './animate-click.directive';

describe('AnimateClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateClickDirective();
    expect(directive).toBeTruthy();
  });
});
